{
  "name": "edge-tts-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Deploy Microsoft Edge TTS as an OpenAI-compatible API on your own Cloudflare account",
  "scripts": {
    "postinstall": "node scripts/extract-mpg123-wasm.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "wrangler": "^4"
  },
  "license": "AGPL-3.0",
  "dependencies": {
    "mpg123-decoder": "^1.0.3"
  }
}
